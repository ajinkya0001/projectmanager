services:

  backend:
    build: ./projectmanager
    container_name: backend
    ports:
      - "8080:8080"

  frontend:
    build: ./projectmanager-ui
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      - backend